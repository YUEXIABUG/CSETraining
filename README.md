# 考公速算训练营（资料分析）

> 🔗 在线地址：**[https://csetraining.yxbug.ren/](https://csetraining.yxbug.ren/)**

基于 **React 19 + Vite + TypeScript + Bootstrap 5** 的公务员考试「资料分析」速算刷题网站。纯前端、无后端依赖、无需数据库，所有题目由内置出题引擎随机生成，克隆即可部署。

---

## 功能一览

### 训练模块

| 模块 | 题型说明 | 判分规则 |
| --- | --- | --- |
| 多位加减法 | 四个三至四位整数加减混合，至多一个减号、至多一个三位数 | 答案必须完全准确 |
| 乘法运算 | 三位整数 × 小于 100% 的一位小数百分比（如 342 × 32.6%） | 允许 1% 以内误差 |
| 分数比大小 | 两个相差 5% 以内的分数，点选 ＞ / ＜ | 选对即得分 |
| 基期与增长量 | 现期量 A + 增长率（**-15% ~ 100%**），同时求基期量与增长量 | 两项各允许 1% 误差，分别判分 |
| 基期比重 · 选择 | 给现期部分 A、整体 B、部分增长率 r<sub>A</sub>、整体增长率 r<sub>B</sub>（均 ±20% 内一位小数），四选一求基期比重 | 选项为位数统一的百分比（至多两位小数） |
| 比重差 · 选择 | 同上条件，四选一判断比重差的方向与数值（A/C 上升、B/D 下降，两组数值相差 20% 以内） | 方向 + 数值均须正确 |
| 套卷模式 | 固定 34 题：4 加减法 + 4 比大小 + 10 乘法 + 2 基期比重 + 2 比重差 + 12 基期增量（6 道 0~20%、2 道 20~100%、4 道 -15~0） | 按各题型规则判分，进度条分模块展示 |

### 答题体验

- **自由跳题**：点击进度条上的任意题号即可切换；「上一题」按钮可回看并修改已答题目
- **进度条三色状态**：🟩 已答 · 🟥 看了没答 · ⬜ 未看；≤10 题为单列，>10 题每 5 题一行，套卷模式分模块显示
- **下一题 / 提交**：非最后一题显示「下一题」，最后一题显示「提交」；填空题可留空直接下一题，选择题另有「先不答」按钮
- **交卷保护**：提交时若存在未答题目，会弹窗列出未答题号，可选「返回作答」或「仍要提交」
- **离开确认**：答题过程中点击「返回」会弹窗确认，防止误触丢失进度
- **逐题计时**：每题单独累计用时（跳题来回切换也会正确累计）+ 全组总计时实时展示
- **每组题数自选**：单项训练默认 5 题，支持 1–100 自定义（套卷模式固定 34 题）
- **亮暗主题**：右下角悬浮按钮一键切换，自动记忆偏好

### 成绩单

- 正确率（基期与增长量分别统计）、总用时、平均每题用时
- **饼图**：答对 / 答错 / 未作答分布；**折线图**：每题用时趋势（套卷模式另有分模块正确率）
- 逐题记录卡片：题号、题目、我的答案、正确答案（含计算过程）、对错、用时；未作答明确标注

---

## 使用方法

1. 打开 [https://csetraining.yxbug.ren/](https://csetraining.yxbug.ren/)
2. 首页选择「每组题数」（仅影响单项训练模块），点击任意模块卡片进入练习；或点击「套卷模式」开始整卷测试
3. 逐题作答：
   - 填空题输入答案后点「下一题」（回车键同效），可留空跳过
   - 选择题点选选项后点「下一题」，也可点「先不答」跳过
   - 随时点击进度条题号或「上一题」回看、修改答案
4. 最后一题点「提交」；如有未答题目会弹窗提醒，确认后生成成绩单
5. 成绩单页可查看图表分析与逐题详解，点「再来一组」生成全新一组题目

---

## 本地部署

要求：[Node.js](https://nodejs.org/) ≥ 18（含 npm）。

```bash
# 1. 克隆仓库
git clone https://github.com/YUEXIABUG/CSETraining.git
cd CSETraining

# 2. 安装依赖
npm install

# 3. 开发模式（热更新）
npm run dev
# 浏览器访问 http://localhost:5173

# 4. 生产构建（类型检查 + 打包，产物在 dist/）
npm run build

# 5. 本地预览生产构建
npm run preview

# 6. 运行出题引擎与页面流程的单元测试（vitest）
npm test
```

### 部署到服务器 / 静态托管

`npm run build` 生成的 `dist/` 是纯静态文件，放到任意静态服务器即可。Nginx 参考配置（SPA 需将路由回退到 index.html）：

```nginx
server {
    listen 80;
    server_name csetraining.example.com;

    root /var/www/csetraining/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

也可以直接部署到 Vercel / Netlify / GitHub Pages / Cloudflare Pages 等平台（构建命令 `npm run build`，输出目录 `dist`）。

---

## 技术栈

- React 19 + TypeScript + Vite 6
- Bootstrap 5 + Bootstrap Icons（亮暗主题）
- 图表为手写 SVG（饼图 / 折线图），无额外图表依赖
- Vitest + Testing Library 单元测试

## 目录结构

```
src/
├── App.tsx                 # 路由
├── meta.ts                 # 模块元信息（标题/图标/描述）
├── types.ts                # 题型与作答记录类型
├── theme.ts                # 亮暗主题
├── components/
│   ├── QuestionViews.tsx   # 各题型答题视图（受控组件）
│   ├── ResultCells.tsx     # 成绩单单元格 / 记录卡片内容
│   ├── Charts.tsx          # SVG 饼图、折线图
│   └── ThemeToggle.tsx     # 右下角主题切换按钮
├── pages/
│   ├── HomePage.tsx        # 首页（模块入口 + 套卷横幅）
│   └── TrainingPage.tsx    # 答题页 + 成绩单
└── utils/
    ├── generators.ts       # 出题引擎（含套卷组卷）
    └── display.ts          # 格式化工具
```

---

## 更新日志

### 2026-08-20 · 大版本更新

**新增**

- 🆕 **基期比重选择题**：现期部分/整体 + 双方增长率（±20% 一位小数），四选一求基期比重，选项为位数统一的百分比
- 🆕 **比重差选择题**：四选项结构为 A 上升 x / B 下降 x / C 上升 y / D 下降 y（x、y 相差 20% 以内），判断方向与数值
- 🆕 **套卷模式**：34 题整卷（4 加减法 + 4 比大小 + 10 乘法 + 2 基期比重 + 2 比重差 + 12 基期增量），进度条分模块显示；其中基期增量含 6 道 0~20%、2 道 20~100%、4 道 -15~0
- 🆕 成绩单新增**饼图**（答对/答错/未答分布）与**折线图**（每题用时趋势）分析模块，套卷模式额外提供分模块正确率
- 🆕 选择题「先不答」按钮；提交时未答题目弹窗提醒（列出题号，可返回补答）
- 🆕 答题中点击「返回」弹出离开确认框

**改进**

- ✨ 答题页支持自由跳题：点击进度条题号切换、「上一题」回看并修改答案，每题用时跨跳转正确累计
- ✨ 进度条三色状态：已答（绿）/ 看了没答（红）/ 未看（灰）；≤10 题单列、>10 题每 5 题一行
- ✨ 非最后一题按钮为「下一题」，最后一题为「提交」；填空题允许留空跳过
- ✨ 基期与增长量增长率扩展为 **-15% ~ 100%**（支持负增长）
- ✨ 成绩单「#」改为「题号」，隐藏左上角返回按钮；答题记录改为逐题卡片，阅读更清晰
- ✨ 界面布局铺满全页，亮暗主题切换按钮移至右下角悬浮

### 更早版本

- 四大基础模块（加减法 / 乘法 / 比大小 / 基期与增长量）、逐题计时、自动判分、成绩单
